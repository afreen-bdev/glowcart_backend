# -------------------------
# APP
# -------------------------
spring.application.name=glowcart-api

# -------------------------
# SERVER PORT (Railway gives PORT automatically)
# -------------------------
server.port=${PORT:8080}

# -------------------------
# DATABASE (Railway PostgreSQL)
# IMPORTANT: must start with jdbc:postgresql://
# -------------------------
spring.datasource.url=jdbc:postgresql://postgres.railway.internal:5432/railway
spring.datasource.username=postgres
spring.datasource.password=MLxzvzkAvWMXpjRGnnSTSuhhEapuMXys

spring.datasource.driver-class-name=org.postgresql.Driver

# -------------------------
# JPA / Hibernate
# -------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Optional (not required but safe)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect