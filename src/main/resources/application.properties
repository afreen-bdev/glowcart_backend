# -------------------------
# APP
# -------------------------
spring.application.name=glowcart-api

# -------------------------
# SERVER (Railway provides PORT)
# -------------------------
server.port=${PORT:8080}

# -------------------------
# DATABASE (Railway PostgreSQL)
# Use PUBLIC URL because postgres.railway.internal works ONLY inside railway private network
# -------------------------
spring.datasource.url=jdbc:postgresql://postgres:Root@123@mainline.proxy.rlwy.net:25310/railway
spring.datasource.username=postgres
spring.datasource.password=MLxzvzkAvWMXpjRGnnSTSuhhEapuMXys
spring.datasource.driver-class-name=org.postgresql.Driver

# Optional but recommended for Railway
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# -------------------------
# JPA / Hibernate
# -------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Not required, but ok to keep
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect